'use strict';

/**
 * transferencia-producto router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::transferencia-producto.transferencia-producto');
